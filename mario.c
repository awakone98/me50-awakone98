#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int a;
    designed to do
    {
       // get user input
       n = get_int("Height: ");
    } while (a < 1 || a > 8);
  
    for (int b = 0; b < a; b++)
    {
              //blankspaces
        for (int c = b - 1; c > b; c--)
            printf(" ");

        // hashetags #
        for (int c = 0; c <= b; c++)
            printf("#");

        printf("\n");    
    }
}    

